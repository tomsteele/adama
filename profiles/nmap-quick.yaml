# top-1000 after discover. ip once per address; fqdn again so each live vhost gets SNI ports.
name: nmap-quick
kinds: [ip, fqdn]
ack_wait: 10m
nmap_args: ["-Pn", "-n", "--open", "--top-ports", "1000"]
